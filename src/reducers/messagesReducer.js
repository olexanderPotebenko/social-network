let initial_state = {
            dialogs: [],
            messages: []
        };

let messagesReducer = (state = initial_state, action) => {
    switch (action.type) {
        default: return state;
    };
};

export default messagesReducer;

