import React from 'react';
import cls from './Item.module.css';
import {NavLink} from 'react-router-dom';

const Item = (props) => {

    return (
        <div className={cls.link__container}>
            <NavLink activeClassName={cls.active} to={props.link}>{props.text}</NavLink>
        </div>
    );
}

export default Item;
