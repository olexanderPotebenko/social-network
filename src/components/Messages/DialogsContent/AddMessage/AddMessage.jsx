import React from 'react';
import cls from './AddMessage.module.css';

const AddMessage = (props) => {
    return (
        <div className={cls.add_message_wrapper}>
            <div className={cls.add_message}>
                <textarea />
                <button />
            </div>
        </div>
    )};

export default AddMessage;
