import React from 'react';
import cls from './Poster.module.css';

const Poster = (props) => {

    return (
        <div className={cls.wrapper}>
            <img className={cls.poster} />
        </div>
    );
};

export default Poster;
